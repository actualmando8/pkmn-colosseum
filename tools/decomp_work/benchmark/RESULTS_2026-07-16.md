# Compile-loop model pilot — 2026-07-16

## Outcome

Kimi K3 successfully participated in the exact-MWCC/objdiff feedback loop. It
produced no improvement or exact match on the initial three-function pilot, but
a later, separate semantic-feedback pilot moved `GStextureLockImage` from a
91.0% baseline to an exact isolated object in two rounds.

This is a small engineering pilot, not a general model leaderboard: K3 was
tested on three functions, Kimi K2.7 Code and a bounded Codex agent on two shared
functions, and no condition used repeated independent trials. The semantic
follow-up used one additional function under a different feedback condition.
It demonstrates that K3 can act on this combined feedback, not that K3 is
generally superior or ready for broad farm deployment.

## Live replacement-key availability check

On 2026-07-17 UTC, the replacement Moonshot credential authenticated and the
provider's `/v1/models` response included `kimi-k3`. Three current-runner
attempts targeted the pinned 90.0% `fn_8009A92C` work unit with two
compile-feedback rounds budgeted. A fourth exercised the final dual-oracle
preflight on `GStextureLockImage` before requesting a completion. All four
stopped before inference with Moonshot `429 engine_overloaded_error`; no
completion or usage record was returned. These runs establish credential/model
access and a transient serving failure only, so they are excluded from every
quality and cost comparison.

The retained initial runs predate prompt path normalization: objdump's file
header embedded each scratch run directory, so those prompts were semantically
equivalent but not byte-identical. The hardened runner now invokes `target.o`
relative to the work unit and hashes each outbound payload. Treat the initial
cross-model comparison as directional.

## Comparable results

All API rows used two rounds. “Primary compile” counts only a complete function
returned in the model's final-content channel. C recovered from a provider's
reasoning channel is reported separately as diagnostic salvage and cannot
improve the primary score.

| Harness / model | Function | Primary compiles | Reasoning salvage | Baseline | Best primary | Best proposal | API wall | Tokens |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| Moonshot `kimi-k3` | `OSAllocFromArenaLo` | 2 / 2 | — | 99.090910% | 99.090910% | 98.181816% | 53.2 s | 5,355 |
| Moonshot `kimi-k3` | `fn_800B7D3C` | 0 / 2 | 2 / 2 | 95.000000% | 95.000000% | 95.000000% salvage | 249.8 s | 15,168 |
| Moonshot `kimi-k3` | `fightTypeGetFightSideFightOutPokemonMax` | 1 / 2 | — | 98.750000% | 98.750000% | 98.750000% | 218.0 s | 20,630 |
| Moonshot `kimi-k2.7-code` | `OSAllocFromArenaLo` | 0 / 2 | 2 / 2 | 99.090910% | 99.090910% | 98.181816% salvage | 162.7 s | 14,273 |
| Bounded Codex agent | `OSAllocFromArenaLo` | 2 / 2 | — | 99.090910% | 99.090910% | 98.181816% | not captured | not captured |
| Bounded Codex agent | `fightTypeGetFightSideFightOutPokemonMax` | 2 / 2 | — | 98.750000% | 99.062500% | 99.062500% | not captured | not captured |

Each Codex comparator was restricted to the same work unit and exactly two
candidate compilations. Candidate compile times were 0.143/0.151 s on
`OSAllocFromArenaLo` and 0.15/0.18 s on `fightType…`; model wall time was not
instrumented, so it is intentionally absent.
On `fightType…`, its second candidate improved by 0.3125 points and left six
register-allocation differences. That attempt was not fully blind: a read-only
search after candidate one exposed a pre-existing 3090 partial for the same
function. Treat the lift as recoverable campaign evidence, not an independent
model-quality observation.

The exposed 3090 artifact is the ignored, uncommitted
`build/permuter_results/3090/_partials/fightTypeGetFightSideFightOutPokemonMax/best.c`
(`best_score=25`, `base_score=40`). It is a NOWIN partial with unsafe `u16` to
`u8` index narrowing, not a landable result. The 99.0625% Codex candidate is
distinct, preserves a `u16` comparison value, and currently exists only in
temporary comparator output; it is not banked or committed.

Using Moonshot's published K3 global pricing of $3/M uncached input, $0.30/M
cached input, and $15/M output, the initial two retained K3 runs cost approximately
$0.038 and $0.141 respectively. Pricing and K3's 1M-context/API constraints are
documented in the [K3 quickstart](https://platform.kimi.com/docs/guide/kimi-k3-quickstart)
and [model catalog](https://platform.kimi.com/docs/models).
The additional `fightType…` run cost approximately $0.146 under the same
published rates.

## Targeted semantic-feedback pilot

The follow-up run at
`build/model_benchmark/20260717T012212.377061Z_moonshot_kimi-k3` tested one
integer/pointer leaf, `GStextureLockImage`, with exact MWCC compilation,
objdiff, and compact Dolphin-interpreter state mismatches from the pinned
ModernGekko dependency tree. This was a separate condition from the comparison
table above and predates the final native-original qualification requirement.

| Stage | Objdiff match | Semantic result (`GStextureLockImage-v2`) |
|---|---:|---:|
| Baseline | 91.000000% | 132 mismatches / 64 fixtures |
| Round 1 | 99.750000% | 132 mismatches / 64 fixtures |
| Round 2 | 100.000000% | 0 mismatches / 64 fixtures |

Round one is an important negative control: a large objdiff improvement did not
repair the wrong return value and reference-count offset. The compact mismatch
feedback named those observable differences, and round two compiled to the
exact 48-byte retail text. The current protocol replay in
`build/semantic_oracle/native-integrated-GStextureLockImage-1000.report.json`
passed 1,000 fixtures with zero native-original-vs-Dolphin mismatches and zero
exact-candidate mismatches. Its candidate and retail `.text` SHA-256 values both equal
`c16b4571e5b98b5da3dd3f66e8e36f653820a5c4e6dee8b4dde8e67739d60a91`.

The two API calls took 99.746 and 101.054 seconds (200.800 seconds total). The
provider-reported usage was:

```json
{
  "round_1": {
    "prompt_tokens": 3235,
    "completion_tokens": 2546,
    "completion_tokens_details": {"reasoning_tokens": 2471},
    "total_tokens": 5781
  },
  "round_2": {
    "prompt_tokens": 6435,
    "prompt_tokens_details": {"cached_tokens": 3072},
    "cached_tokens": 3072,
    "completion_tokens": 2730,
    "completion_tokens_details": {"reasoning_tokens": 2582},
    "total_tokens": 9165
  }
}
```

At the published K3 rates cited above, that is an estimated $0.100: 6,598
uncached input tokens, 3,072 cached input tokens, and 5,276 output tokens. This
estimate assumes reasoning tokens are billed as output tokens.

K3's exact proposal used aliasing casts to address fields at offsets absent from
the supplied stale structure declaration. The source-authentic landing instead
corrects `GStextureHandle` and uses ordinary `mipData` and `refCount` member
access; it does not retain those casts. The source-tree objdiff report confirms
`GStextureLockImage` at 100.0%, adding one matched function and 48 matched code
bytes, and the normal full build passes the canonical DOL SHA-1 check. The
surrounding `gs_texture` translation unit remains partial, so this banks
function progress without making a newly linkable unit.

## Exploratory diagnostics

Before the retained runs, `fn_80034708` was tried with output caps from 1,024
through 16,384 tokens. K3 used essentially every available completion token for
reasoning and returned no complete final-content function. Approximate API wall
times rose from 31 s at 1,024 tokens to 226 s at 8,192 and 469 s at 16,384.
These schema/debug trials are excluded from the table and any quality claim.

A hardened baseline preflight also excluded two nominal suite entries before
any model request. `HSD_Index2LightID` advertised 99.642860% but recomputed at
99.107140%; `heroMoveSetNeckMode` advertised 94.986000% but recomputed at
94.202896%. Both were removed from `suite_pilot.json` rather than silently
changing the benchmark denominator. A bounded two-candidate Codex diagnostic
on `HSD_Index2LightID` tied the recomputed 99.107140% baseline, but is excluded
from the comparable table because the work-unit metadata was inconsistent.

The `fn_800B7D3C` and K2.7 runs originally compiled C found inside streamed
reasoning when final content was empty. The hardened runner now labels this
explicitly as `reasoning_salvage`, excludes it from primary metrics and primary
feedback, and never persists raw reasoning text.

Direct `deepseek-v4-pro` and `glm-5.2` comparator requests reached their
providers but stopped before inference because those accounts had no spendable
balance/resource package. The configured OpenRouter account also had no
spendable balance. Those are infrastructure failures, not model scores.

## Current-master K3 retest — 2026-07-21

The replacement Moonshot credential was retested from clean master
`ec451007`. The authenticated model catalog listed `kimi-k3`, and that exact
served-model identifier handled all three requests. The fidelity-gated ignored
work unit was `GSmodelSetShadowTextureSize`, a 72-byte function at a
94.166664% baseline (objdiff score 300). It did not overlap PR #414, the active
Windows queue, or either of the other two reserved K3 work units.

| Round | Result | Match | Wall | Prompt | Cached | Completion | Reasoning |
|---:|---|---:|---:|---:|---:|---:|---:|
| 1 | compiled, policy-clean tie | 94.166664% | 287.534 s | 1,419 | 0 | 8,142 | 7,902 |
| 2 | compiled tie; rejected `goto` | 94.166664% | 226.395 s | 9,995 | 1,280 | 6,343 | 6,061 |
| 3 | output limit; no complete function | — | 292.535 s | 16,776 | 9,984 | 8,192 | 8,189 |

Aggregate usage was 50,867 tokens over 807.569 seconds: 28,190 prompt,
22,677 completion, and 11,264 cached prompt tokens. At the published K3 rates
used elsewhere in this report, the estimated cost is $0.394312. There was zero
match lift, zero isolated exact result, and no source-tree patch. The temporary
owner-only credential copy was removed at process exit; no credential was
written to the repository, environment, or command line.

This retest strengthens the deployment gate: do not broad-farm K3 on plain
objdiff leaves. Use one round and stop on zero lift. Reserve maximum-reasoning
K3 calls for qualified semantic-oracle cases, where the earlier
`GStextureLockImage` result shows that compact behavioral mismatches can add
useful information beyond compiler diff feedback.

## What the pilot establishes

- The active work-unit generator, exact MWCC command, objdiff scoring, and
  iterative compiler/diff feedback can be composed into a reproducible model
  test.
- K3 was more operationally usable than K2.7 Code on the tiny shared leaf:
  on `OSAllocFromArenaLo`, K3 returned final C in both rounds and completed in
  roughly one third of the API wall time. With one shared function, this is only
  a directional signal.
- All three harnesses converged on the same worse operand/alignment shape for
  `OSAllocFromArenaLo`; none beat the incumbent.
- K3 tied the `fightType…` incumbent after one no-final round; the bounded Codex
  lane found a nonblind 99.0625% lead that still needs normal source-tree and
  full-build validation.
- In a separate one-function semantic condition, K3 converted compact
  behavioral mismatches plus compiler/objdiff feedback into an exact
  `GStextureLockImage` object by round two. This is workflow evidence, not a
  cross-model quality result.
- No benchmark candidate was linked or accepted as campaign progress during
  the initial comparison. The later semantic candidate passed the normal
  source-tree and full-DOL acceptance gates and banked one exact function; its
  surrounding partial translation unit is not newly linkable.

## Next benchmark gate

Before farm deployment, run at least 30 held-out functions stratified by size
and control-flow shape, with three independent attempts per model and separate
single-shot and compile-feedback conditions. Pin source commit, work-unit hash,
tool hashes, prompt, sampling parameters, and attempt budget.

Primary acceptance metrics should be exact matches, compile-at-1, match lift,
integrated object/link success, full-build regressions, wall time, tokens, and
cost. A practical farm gate is at least three exact held-out wins or a
statistically credible match lift without source-policy violations. Comparator
accounts must be funded before the multi-model result can be called general.
