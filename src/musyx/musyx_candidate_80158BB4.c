/**
 * @file musyx_candidate_80158BB4.c
 * @brief Candidate-only suffix view of the residual MusyX runtime.
 *
 * The implementation remains centralized in musyx_range_80157280.c while the
 * target text is partitioned around the exact voice bookkeeping island. This
 * candidate wrapper is never linked as decompilation progress.
 */
#include "src/musyx/musyx_range_80157280.c"
