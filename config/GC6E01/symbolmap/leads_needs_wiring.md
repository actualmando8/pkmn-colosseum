# Confirmed-name leads needing signature/typing work

2 remaining (of the original proposals). These names are confirmed but the asm-wrapper `(void)` signature / untyped `lbl_` globals / a name collision block a clean bulk rename — each needs per-function decomp work (match the real prototype, type its globals). Wired leads have been moved to applied_symbols.txt.

| addr (fn_) | confirmed name | provenance | header proto in |
|---|---|---|---|
| `fn_801BBFE4` | HSD_TObjSetup | XD port (score 2.25) | C:/Users/douglaswhittingham/pkmn-colosseum/include/hsd/hsd_tobj.h |
| `fn_80137AA4` | tracefxStartEffect | string self-name (leading) | C:/Users/douglaswhittingham/pkmn-colosseum/include/game/effect/gs_effect.h |
