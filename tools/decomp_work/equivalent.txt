# Equivalent-function registry — the Pikmin2/TWW "Equivalent" tier for this project.
#
# A function listed here has REAL, CORRECT, decompiled C that does NOT byte-match
# because it hits a confirmed, C-uncontrollable wall (see WALLS.md for the class).
# It counts toward the C-converted axis and is a HARD-SKIP: do not re-grind it.
#
# Format: one `fn_XXXXXXXX` per line; everything after `#` is a comment.
# Add a function here ONLY after it is logged in WALLS.md (class confirmed, or
# tested-and-didn't-move). Read by tools/decomp_work/progress2.py.
#
# Kept OUT of config/GC6E01/ (build-truth dir) so no build glob can ingest it.

fn_800E3604   # gs_material      — W1 data-flow-locked reg-alloc (live-range coalescing); 99.37%, inversion-tested, locked
fn_8023CE60   # colosseum_script — W1 param landed one saved-reg off (data-flow-locked); 98.26%, inversion-tested, locked
fn_80129280   # gs_field_world   — W6 jumptable NAME only (@6029 vs jumptable_803634A8); 99.85%, bytes link identical; annealer isolated frame scores 0
fn_80132A38   # effect_util      — W6 jumptable NAME only (@559 vs jumptable_80363630); 99.92%, bytes link identical
fn_80029FAC   # gs_worldmap       — W2 scheduling (SDA-load-first issue order); 97.01%, exhaustively swept 2026-06-10
fn_8002A0B8   # gs_worldmap       — W2 scheduling, sibling of fn_80029FAC (same wall)
fn_80065A48   # ui_core          � W6 jumptable NAME only (@2187 vs jumptable_802EDB7C); 99.99%, bytes link identical
fn_800E4170   # gs_material      — W1 register-recycling (linear-scan slot reuse in section 3); 94.3%, C is correct
fn_800E8EFC   # gs_material      — W2 prologue scheduling (addi via r0 vs direct addi r30); 92.9%, C is correct
fn_800E8FA0   # gs_material      — W-peephole bgtlr-fusion (ble @L; blr not reproducible); 88.9%, C is correct
fightSideGetStatus	# pokemon.c 99.82%: instructions byte-exact; sole diff = CW anonymous local jumptable symbol vs target's named jumptable_8037564C (dtk-named). Unreachable from C.
fn_800218BC   # gs_title.c    — W2 instruction scheduling (lfs f31 before li r31 at loop-init; CW issues SDA load first, target issues zero-init first); 98.35%, C is correct
fn_800D13C8   # gs_render_util.c — W2 instruction scheduling (lis r4 between lis r3 / addi r6 in HSD_ForeachAnim call setup) + W-SDA-RELOC-C (lfd @sda21(r0) vs -27880(r2)); 99.4%, C is correct
fn_80005E00   # main.c — W1 volatile-FPR rotation + anonymous conversion-literal reloc artifact; 99.39% parent-verified (2 independent agents), TaskRetraceMain C is correct
fn_800A4C94   # DVDLow.c — W-operand-order: target emits `lis r3`(addr) before `li r0`(value) in the 0xCC006004 store; CW emits value-first across ALL 7 CW versions (1.1–2.0), ~20 phrasings swept; 71.4% parent-verified, DVDCBCallback read-and-clear getter C is correct (un-wrapped from codex's undocumented asm-flip 2708d871)
fn_801A6DDC   # hsd_mobj.c — W-peephole branch-merge: CW fuses `bne L; blr` -> `beqlr`; target keeps the inner `mat` null-check un-merged; 77.8% parent-verified (20%->77.8% via #pragma optimization_level 1 + guard-clause restructure), HSD_MObj alpha-setter C is correct (un-wrapped from codex 2708d871)

# colosseum_script.c confirmed reg-alloc/scheduler walls (>=95%, correct C, 2026-06-14)
fn_80212D6C	# 96.16%
fn_80214B68	# 98.95%
fn_80216410	# 95.75%
fn_8021847C	# 98.46%
fn_8021C308	# 98.32%
fn_8021E754	# 97.64%
fn_80222ADC	# 95.00%
fn_80228DAC	# 99.18%
fn_80229C90	# 96.28%
fn_802389D4	# 97.76%
fn_8023CFDC	# 98.26%

fn_80162FB0   # people_field.c - float-conversion-in-TU: readable typed C active; match_scan 78.3%, single-symbol diff 98.48%; only meaningful residual is CW anonymous @405 u32->f32 magic double vs target lbl_8047D4E8

# === 2026-06-15: 3 colosseum_event walls already confirmed/tested in WALLS.md,
# === promoted to registry (were missing). Other walltriage-classified walls staged in _wall_candidates.md.
fn_8020B330   # colosseum_event.c - W1 saved-band tie-break locked at opt4 (7 variants inert) - WALLS.md
fn_8020341C   # colosseum_event.c - W1 saved-vs-temp tie-break (ANNEAL sweep inert) - WALLS.md
fn_8020EED4   # colosseum_event.c - W1 saved-band tie-break (comma-op/param-split inert at opt4) - WALLS.md
